# Firefox Configuration

## Dark theme everywhere

1. Go to `about:config`
2. Set `browser.in-content.dark-mode` to `true`
3. Set `ui.systemUsesDarkTheme` to `1` 
4. Set `ui.allow_platform_file_picker` to `true` so that filemanager uses gtk themed 
