# Installing a Shell

Check current shell

```bash
$ echo $SHELL
```

List existing Shells

```bash
$ chsh -l
```

Install zsh on Archlinux

```bash
$ sudo pacman -S zsh
```

Change shell

```bash
$ chsh -s /bin/zsh
```

or 

```bash
$ chsh -s /bin/bash
```